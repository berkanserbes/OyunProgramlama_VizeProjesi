using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArenaSc : MonoBehaviour
{
    PlayerSc playerSc;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Arena konum: " + this.transform.position);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
    void FixedUpdate() {

    } */
}
